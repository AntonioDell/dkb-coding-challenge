{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Nuxt App (dev)",
      "type": "shell",
      "command": "pnpm dev",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
